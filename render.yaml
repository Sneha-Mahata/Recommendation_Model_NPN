# render.yaml (optional - only if not using Docker)
services:
  - name: hotel-recommender
    type: web
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k uvicorn.workers.UvicornWorker app:app -b 0.0.0.0:$PORT
